#!/usr/bin/python3

def raise_exception_msg(message=""):
    """
    Raise a NameError exception with a given message.

    Args:
        message (str): The custom error message (default is an empty string).
    """
    raise NameError(message)

